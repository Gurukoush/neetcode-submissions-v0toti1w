class Solution:
    def getConcatenation(self, arr: List[int]) -> List[int]:

        temp = []
        for i in range(len(arr)):
            temp.append(arr[i])
        for i in range(len(arr)):
            temp.append(arr[i])
        return temp


        
        