class Solution:
    def getConcatenation(self, arr: List[int]) -> List[int]:
        #n = len(arr)
        k = []
        for i in range(2):
            for n in arr:
                k.append(n)
        return k

        
        